// hooks/useAccessibility.ts
'use client';

import { useAccessibility as useAccessibilityBase } from '@/app/accessibility/theme-provider';

export const useAccessibility = () => useAccessibilityBase();